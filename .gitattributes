# Windows batch files must always be CRLF — cmd.exe mis-parses LF-only .bat
# (splits setlocal/cd/echo lines into fragments). Pin them regardless of the
# user's core.autocrlf setting.
*.bat text eol=crlf
*.cmd text eol=crlf
