network_dim = 32
network_alpha = 32
learning_rate = 2e-5
max_train_epochs = 4
save_every_n_epochs = 2
checkpointing_epochs = 2
use_shuffled_caption_variants = true
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1
output_name = "lokr"
blocks_to_swap = 0

use_lokr = true
lokr_factor = -1
decompose_both = false

[variant]
family = "lora"
label = "LoKr"
description = "Low-Rank Kronecker product factorization — compact ΔW via kron(w1, w2)."
order = 40
target_res = [ 896, 1024,]
