network_dim = 64
network_alpha = 64
learning_rate = 1e-4
max_train_epochs = 6
save_every_n_epochs = 3
checkpointing_epochs = 3
use_shuffled_caption_variants = true
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1
output_name = "vera"
blocks_to_swap = 0

use_ve = true
vera_seed = 0

[variant]
family = "lora"
label = "VeRA"
description = "Vector-based Random Matrix Adaptation — ~10-100x fewer trainable params than LoRA."
order = 60
target_res = [ 896, 1024,]
