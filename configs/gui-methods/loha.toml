network_dim = 32
network_alpha = 32
learning_rate = 1e-4
max_train_epochs = 4
save_every_n_epochs = 2
checkpointing_epochs = 2
use_shuffled_caption_variants = true
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1
output_name = "loha"
blocks_to_swap = 0

use_loha = true

[variant]
family = "lora"
label = "LoHa"
description = "Low-rank Hadamard product — ΔW via (w1a@w1b) ⊙ (w2a@w2b), effective rank up to r²."
order = 70
target_res = [ 896, 1024,]
