"""MonadForge system-tray app — a status indicator + controller for the daemon.

Run with ``pythonw -m scripts.tray`` (no console flash) or the installed
``monadforge-tray`` gui-script. See :mod:`scripts.tray.app` for the app and
:mod:`scripts.tray.icons` for the procedural brand-mark icons.
"""
